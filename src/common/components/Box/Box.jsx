import styled from 'styled-components';
import { color, space, layout, border, flexbox } from 'styled-system';

const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${border}
  ${flexbox}
`;

export default Box;
